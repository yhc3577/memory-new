# pnpm 11+ allow-list for native build scripts.
# Required so `better-sqlite3` (a native module via node-gyp) can compile on install.
# Without this, pnpm exits with ERR_PNPM_IGNORED_BUILDS and the npm postinstall
# (which patches the visualize config) never runs.
onlyBuiltDependencies[]=better-sqlite3